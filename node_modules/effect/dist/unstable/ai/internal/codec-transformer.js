import * as Schema from "../../../Schema.js";
import * as ServiceMap from "../../../ServiceMap.js";
/** @internal */
export const defaultCodecTransformer = codec => {
  const document = Schema.toJsonSchemaDocument(codec);
  const jsonSchema = document.schema;
  if (Object.keys(document.definitions).length > 0) {
    jsonSchema.$defs = document.definitions;
  }
  return {
    codec,
    jsonSchema
  };
};
/** @internal */
export const CurrentCodecTransformer = /*#__PURE__*/ServiceMap.Reference("effect/unstable/ai/CodecTransformer", {
  defaultValue: () => defaultCodecTransformer
});
//# sourceMappingURL=codec-transformer.js.map